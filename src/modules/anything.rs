#![allow(dead_code)]
// anything.rs
// This is a test layer, please replace it with yours.

use crate::datastruct::RequestParam;

pub fn entry(request_param: RequestParam) -> RequestParam {
    return request_param;
}